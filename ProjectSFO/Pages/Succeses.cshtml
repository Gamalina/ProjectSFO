@page
@model ProjectSFO.Pages.Shared.SuccesesModel
@{
}
<script>
    document.addEventListener('DOMContentLoaded', function () {
        var calendarEl = document.getElementById('calendar');

        var calendar = new FullCalendar.Calendar(calendarEl, {
            themeSystem: 'bootstraps5',
            initialView: 'dayGridMonth',
            selectable: true,
            contentHeight: 535,
            select: function (info) {
                alert('Selected: ' + info.startStr + ' to ' + info.endStr);
            },
            headerToolbar: {
                left: 'prev,next today',
                center: 'title',
                right: 'dayGridMonth,timeGridWeek'
            },

        });

        calendar.render();
        
        calendarEl.style.marginLeft = '0px';
        
    });
</script>



    <div class="fluid-container" style="margin-left:30px;">
    <div class="row" style="max-width:1860px;">
        <nav class="col-md-2 col-lg-2 d-md-block bg-light sidebar">
            <div class="sidebar-sticky">
                <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
                    Filter
                </h6>
                <ul class="nav flex-column">
                    <li class="nav-item">

                        <label class="nav-link">
                            <input type="checkbox" class="category-toggle form-check-input" checked>
                            BEC
                            <i class="fas fa-angle-down" data-toggle="collapse" href="#becCheckboxes" aria-expanded="true" aria-controls="becCheckboxes"></i>
                        </label>

                        <ul class="collapse checkboxespadding" id="becCheckboxes">
                            <li class="nav">
                                <!-- The collapsible content -->
                                @if (Model.SolutionData != null && Model.SolutionData.BECSolutions != null)
                                {
                                    @foreach (var solution in Model.SolutionData.BECSolutions)
                                    {
                                        <label class="nav-link">
                                            <input class="form-check-input" type="checkbox" id="@solution" checked>
                                            @solution
                                        </label>
                                    }
                                }
                            </li>
                        </ul>
                    </li>
                    <li class="nav-item">
                        <label class="nav-link">
                            <input type="checkbox" class="category-toggle form-check-input" data-toggle="collapse" data-target="#BankdataCheckboxes" checked>
                            Bankdata
                        </label>
                        <ul class="collapse checkboxespadding" id="BankdataCheckboxes">
                            <li class="nav">
                                <!-- The collapsible content -->
                                @if (Model.SolutionData != null && Model.SolutionData.BankDataSolutions != null)
                                {
                                    @foreach (var solution in Model.SolutionData.BankDataSolutions)
                                    {
                                        <label class="nav-link">
                                            <input class="form-check-input" type="checkbox" id="@solution" checked>
                                            @solution
                                        </label>
                                    }
                                }
                            </li>
                        </ul>
                    </li>
                    <li class="nav-item">
                        <label class="nav-link">
                            <input type="checkbox" class="category-toggle form-check-input" data-toggle="collapse" data-target="#SDCCheckboxes" checked>
                            SDC
                        </label>
                        <ul class="collapse checkboxespadding" id="SDCCheckboxes">
                            <li class="nav">
                                <!-- The collapsible content -->
                                @if (Model.SolutionData != null && Model.SolutionData.SDCSolutions != null)
                                {
                                    @foreach (var solution in Model.SolutionData.SDCSolutions)
                                    {
                                        <label class="nav-link">
                                            <input class="form-check-input" type="checkbox" id="@solution" checked>
                                            @solution
                                        </label>
                                    }
                                }
                            </li>
                        </ul>
                    </li>
                    <li class="nav-item">
                        <label class="nav-link">
                            <input type="checkbox" class="category-toggle form-check-input" data-toggle="collapse" data-target="#NykreditCheckboxes" checked>
                            Nykredit
                        </label>
                        <ul class="collapse checkboxespadding" id="NykreditCheckboxes">
                            <li class="nav">
                                <!-- The collapsible content -->
                                @if (Model.SolutionData != null && Model.SolutionData.NykreditSolutions != null)
                                {
                                    @foreach (var solution in Model.SolutionData.NykreditSolutions)
                                    {
                                        <label class="nav-link">
                                            <input class="form-check-input" type="checkbox" id="@solution" checked>
                                            @solution
                                        </label>
                                    }
                                }
                            </li>
                        </ul>
                    </li>
                    <li class="nav-item">
                        <label class="nav-link">
                            <input type="checkbox" class="category-toggle form-check-input" data-toggle="collapse" data-target="#JyskebankCheckboxes" checked>
                            Jyskebank
                        </label>
                        <ul class="collapse checkboxespadding" id="JyskebankCheckboxes">
                            <li class="nav">
                                <!-- The collapsible content -->
                                @if (Model.SolutionData != null && Model.SolutionData.JyskebankSolutions != null)
                                {
                                    @foreach (var solution in Model.SolutionData.JyskebankSolutions)
                                    {
                                        <label class="nav-link">
                                            <input class="form-check-input" type="checkbox" id="@solution" checked>
                                            @solution
                                        </label>
                                    }
                                }
                            </li>
                        </ul>
                    </li>

                </ul>
            </div>
        </nav>

            <div class="col-md-10">
                    <div class="col-md-12" id='calendar'></div>
            </div>
        </div>
    </div>




